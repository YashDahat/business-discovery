== FEATURE BEING ENRICHED ==
Feature name: {{featureName}}
Feature display name: {{featureDisplayName}}
Feature type: {{featureType}}

== THIS FEATURE'S FILES (outline — you fill in the structural detail for exactly these paths) ==
{{featureFiles}}

== OTHER FEATURES' API CONTRACTS (what this feature may call — do not invent signatures absent from here) ==
{{peerApiSummaries}}
{{dependencyDirectionSection}}

== BUSINESS CONTEXT ==
Business: {{businessName}} ({{category}})
Address: {{address}}
Phone: {{phone}}
Coordinates: {{latitude}}, {{longitude}}
Opening hours: {{openHours}}
Tech stack: {{techStack}}

== DESIGN CONTEXT ==
Visual direction: {{designDirection}}
Color palette: {{colorScheme}}
Tone: {{tone}}

Apply design context when writing about PAGE and COMPONENT files in this feature. Translate visual direction into named page sections; translate color palette into exact Tailwind arbitrary-value classes; use tone to guide placeholder copy in CTAs and headings.
{{requestedChangesSection}}
Write the featureInstruction for {{featureDisplayName}} AND the per-file structural
detail ("files" array — one entry per file listed above, matched by exact file_path)
following all rules in the system prompt. Call read_file(path) for any existing
workspace file you need to inspect before writing.

Return ONLY the JSON object with "feature_instruction", "depends_on_features", "files"
(and "change_required" if requestedChanges was provided). No markdown fences. No prose.
